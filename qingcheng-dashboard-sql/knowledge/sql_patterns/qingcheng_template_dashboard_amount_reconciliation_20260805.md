# 青橙看板与模板取数金额五期核对（2026-08-05）

## 1. 结论

在员工范围一致、金额单位统一为元、按个人和团队分别聚合的前提下，`<个人转化数据-青橙>` 与 `<团队转化完成度-青橙>` 当前展示的全部收入/退款字段，与保留的 `<H业务线*青橙项目部*渠道订单流水明细表>` 模板逻辑 **不能在所有期次逐分一致**。

差异不是团队汇总或员工范围错位：五个期次的个人键集合、团队键集合完全一致，差异集中在 10 个员工/团队键；`20260803期` 个人和团队两个粒度的收入、退款均已逐分一致。当前五期合计为：

| 粒度 | 模板收入 | 看板收入 | 收入差异（看板-模板） | 模板退款 | 看板退款 | 退款差异（看板-模板） |
|---|---:|---:|---:|---:|---:|---:|
| 个人（575 个键） | 11,841,407.12 | 11,841,082.72 | -324.40 | 1,452,160.60 | 1,452,071.80 | -88.80 |
| 团队（110 个键） | 11,841,407.12 | 11,841,082.72 | -324.40 | 1,452,160.60 | 1,452,071.80 | -88.80 |

团队合计与个人合计相同，是因为团队键对个人业绩进行了完整汇总，并非两份数据集偶然相同。

## 2. 五个期次结果

| 期次 | 个人/团队键数 | 模板收入 | 看板收入 | 收入差异 | 模板退款 | 看板退款 | 退款差异 | 是否逐分一致 |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| 20260710期 | 个人 115 / 团队 22 | 4,120,279.50 | 4,120,072.00 | -207.50 | 406,153.34 | 406,124.34 | -29.00 | 否 |
| 20260716期 | 个人 115 / 团队 22 | 2,687,636.34 | 2,687,577.44 | -58.90 | 316,168.74 | 316,138.84 | -29.90 | 否 |
| 20260722期 | 个人 115 / 团队 22 | 2,513,676.99 | 2,513,647.99 | -29.00 | 200,325.09 | 200,295.19 | -29.90 | 否 |
| 20260728期 | 个人 117 / 团队 22 | 1,621,314.29 | 1,621,285.29 | -29.00 | 399,013.93 | 399,013.93 | 0.00 | 否 |
| 20260803期 | 个人 113 / 团队 22 | 898,500.00 | 898,500.00 | 0.00 | 130,499.50 | 130,499.50 | 0.00 | 是 |

其中收入绝对差异占模板收入约 `0.00274%`，退款绝对差异占模板退款约 `0.00612%`。这说明当前不是“退款整体超过 20%”或大范围员工错配问题，而是少量明细的口径差异。

## 3. 根因定位

### 3.1 模板保留试听订单，看板 SQL 排除了试听订单

模板原始 SQL `resources/raw_sql/qingcheng_channel_order_detail_raw_20260627.sql` 的 `gmv` 层限定业绩二级部门为青橙，但没有排除 `clazz_name like '%试听%'` 的流水。

当前三个完成度数据集的 `service_base0` 明确使用：

```sql
and coalesce(s.clazz_name, '') not like '%试听%'
```

诊断查询对全部差异键重新计算后，模板与看板的差额逐笔等于被排除试听流水的 `income_amount / refund_amount`：

- 非 H 课程损失：所有差异键收入、退款均为 `0`；
- 渠道 `ld` 重复放大：所有差异键重复行数为 `0`，额外收入、退款均为 `0`；
- 试听流水：10 个差异键均可由试听收入或试听退款完全解释。

因此，本次五期核对的直接原因是：**模板把试听订单纳入金额，看板按当前业务定义排除了试听订单。**

### 3.2 不是 finance 源表重复行导致的当前金额差异

`finance_dw.app_finance_performance_extend_details_hf` 确实存在业务键重复行，仍需在 finance 补充和退 4/点睛退 2 规则路径中去重；但当前看板的 `income_all/refund_all` 由 service 明细直接汇总：

```text
income_all = sum(service income_amount / 100)
refund_all = sum(service refund_amount / 100)
```

finance 不参与这两个全部收入/全部退款字段的正常金额求和。当前五期、同一员工范围的诊断也未发现由渠道明细 join 造成的重复放大。因此，**不能通过修改 finance 源表来解决这组看板与模板的差异**。finance 重复仍是需要治理的独立数据质量问题，影响范围主要是旧 `income/refund`、调课调班补充链路和退 4/点睛退 2 规则字段。

## 4. 当前看板金额公式

当前线上数据集为：

- 个人：model `2769`，`resources/raw_sql/data_center_qingcheng_2769.sql`；
- 团队期：model `2680`，`resources/raw_sql/data_center_qingcheng_2680.sql`；
- 团队月：model `2677`，`resources/raw_sql/data_center_qingcheng_2677.sql`。

金额字段的业务公式为：

```text
班课营收 / 团队营收金额 = sum(income_all)
班课退费 / 团队退费金额 = sum(refund_all)
班课净收 / 团队净金额 = sum(income_all) - sum(refund_all)
```

这三个字段不剔除调课调班，也不把退 4/点睛退 2 规则混入全部退款；`transfer_in_amount/transfer_out_amount` 继续用于内部调课识别，`ord/re_ke` 继续用于退 4、点睛退 2 规则。

折算字段保持原业务含义，不用 `income_all/refund_all` 直接替换：

```text
个人折算后产出
= sum(n_H_promit_4) * 0.5
  + (sum(H_promit_4) - sum(Y_promit_4))

团队折算净收款-退4
= sum(n_H_promit_4) * 0.5 + sum(H_promit_4)
```

即：全部收入/全部退款用于看板金额展示；折算产出仍按原有 H/非 H、退 4、点睛退 2 和个人一对一扣除规则计算。

## 5. 与模板对齐建议

若要求模板取数与当前看板业务含义一致，推荐在模板 `gmv` 源查询中增加：

```sql
and coalesce(clazz_name, '') not like '%试听%'
```

这样可以保留当前看板“排除试听”的业务含义，同时使模板与两个看板在本次验证范围内逐分一致。若反过来删除看板的试听过滤，虽然可以向当前模板靠拢，但会改变“班课营收/退费为非试听正常订单金额”的既有业务含义，不建议直接采用。

本次没有修改线上模板或看板；只更新了本地 `qingcheng-dashboard-sql` 的 canonical SQL 与知识文档。模板精确名称未在当前“我的模板-我创建的”列表中检索到，本次比较采用 Skill 中保留的模板原始 SQL，并补齐 USQL 必需的当前青橙课程范围过滤；因此若模板线上版本另有未保留的筛选条件，应以模板回读 SQL 做最终复核。

## 6. 验证证据

- 对比汇总：`C:\Users\Ludim\.codex\runtime\usql-web-query-operator\analysis\template_dashboard_amount_comparison_20260805.json`
- 个人模板结果：query `1530284469`，返回 575 行。
- 个人看板结果：query `1530288537`，返回 575 行。
- 团队模板结果：query `1530298304`，返回 110 行。
- 团队看板结果：query `1530301728`，返回 110 行。
- 差异诊断：query `1530317495`，返回 10 个差异键；每个差异键均由试听金额解释，渠道重复指标为 0。
- 个人看板只读 profile hash：`c79db30f804747cd5f6ff3079b8a5050ab9efc8ae2c46f63de804a07bbb6c369`。
- 团队看板只读 profile hash：`291d536863a1a4bfd23cc3e7403e8b7fe921cd7e4e0a8c1a7fc5beaf755b950f`。
