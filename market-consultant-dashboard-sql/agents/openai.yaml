interface:
  display_name: "市场顾问 Text2SQL"
  short_description: "解析市场顾问及馒头订单模板域契约，生成受控 SQL 与可审计看板设计变更计划。"
  default_prompt: "Use $market-consultant-dashboard-sql only for the market_consultant domain, including the registered 馒头_订单明细_支付时间 and 馒头_订单明细_流水时间 Template Queries. Resolve domain-local semantic contracts, block pending_confirmation items, build and validate QuerySpec and QueryPlan artifacts, then compile only executable metrics with automatic_compile=true or generate bounded read-only probes; never default Qingcheng semantics into this domain. When dashboard design is requested, derive domain-bound DashboardDesignSpec and DashboardChangePlan artifacts without treating them as platform-write authority; keep Apply behind the operator allowlist."
