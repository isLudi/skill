interface:
  display_name: "数据推送与分发"
  short_description: "按部门隔离编排本地脚本与妙搭全栈数据推送、配置和调度"
  default_prompt: "Use $data-push to design or maintain a department-scoped local or Miaoda delivery without mixing configurations, styles, schedules, or ledgers."

policy:
  allow_implicit_invocation: true
