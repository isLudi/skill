# 青橙行课开课时间源表契约

证据快照日期：2026-07-27。运行时必须重新读取群消息与在线文档，不得把本快照视为“当前最新”。

## 来源身份与文档

- 群：`青橙数据对接`，`oc_e604e064976c022ab4289fc2fb979332`
- 发布人：李怡青（`liyiqing02`），`ou_3168c83ffe93b49a192755c8e31e2bc5`
- 已验证消息：`om_x100b69478c6c80a0df62926437bfc84`，2026-07-27 19:08
- 下载后的固定文档标题：`青橙行课--开课时间(1-6节课)`
- 链接仅允许 `https://docs.baijia.com/sheet/...`
- 本地累计表：`E:\1900_work\GAOTU\19003_青橙项目部看板维护表格\qing_daoke.xlsx`
- 平台临时表：`temp_table.dingxi01_qing_daoke`

账号密码只从 `QINGCHENG_DOCS_ENV_FILE` 或 Registry 登记的本地环境文件读取，并且必须限定在 `USQL Web Query (Playwright) credentials` 这一精确配置段。环境文件可能同时包含 Tiangong2 等其他登录配置；不得使用未限定配置段的“最后一个同名键”，也不得在日志、计划、回复、测试夹具或 Git 文件中记录凭据值。下载使用临时浏览器上下文，不复用或持久化浏览器登录状态。

## 群消息文案识别

群消息不要求逐字复用固定文档标题。只要同时满足精确群、李怡青身份、登记链接域名，并且正文包含下列任一行课语义，即可识别为 `course_schedule`：

- `青橙行课`；
- `开课、上课、行课、到课、课程` 与 `时间、安排、信息、表、课表、链接` 的常见组合；
- `课表`，允许词间空白。

例如“辛苦上传上课时间”“最新开课时间表”“青橙行课安排”“本期到课表”均可识别。仅有链接或“上传数据表”等不含行课语义的正文仍拒绝。群消息文案放宽不放宽下载门禁；在线文档打开后仍必须逐字匹配固定文档标题。

## 工作表与字段

每次下载完整 XLSX，只读取当前活动工作表。活动表名必须是源 `qici` 的后缀，例如活动表 `0728期` 只允许 `qici=20260728期`。

| 源字段 | 目标字段 | 处理 |
|---|---|---|
| 渠道 | `qudao` | 原值 |
| 开课时间 | `begin_time` | 折叠重复空白，必须为 `%Y-%m-%d %H:%M:%S` |
| 期次 | `qici` | 必须为 8 位日期加“期”，并与活动表名一致 |
| 工作日-1 | `dow` | 只允许 1 至 7 |
| 年级 | `grade` | 非空 |
| 课节数(第几节课) | `ke_1` | 转为文本，只允许 1 至 6 |
| 工作日 | — | 明确忽略，不写入目标 |

源文件中的公式只用于读取已缓存的有效值。写入 `qing_daoke.xlsx` 时必须物化有效值，禁止把源表的外部链接公式复制到本地维护表。

## 合并与门禁

- 唯一源键：`qici + qudao + grade + ke_1 + begin_time + dow`
- 合并方式：按 `qici` 整期 slice upsert；先移除目标中重叠期次，再插入当前源期次。
- 非重叠历史期次必须保留，目标列顺序固定为 `qudao,begin_time,qici,dow,grade,ke_1`。
- 若有效值与目标整期完全一致，计划必须报告 no-op，不重建工作簿。
- 来源消息最大年龄为 240 小时，来源行数必须为 20–500；新期次相对目标最新期次、修订期次相对目标同一期次的行数变化不得超过 50%。
- `qudao,begin_time,qici,dow,grade,ke_1` 的空值率上限均为 0。任一来源质量门禁失败时，计划必须阻断。
- `plan` 可下载、比较和生成 staged 候选，不改 E 盘、不上传。
- `apply-local` 需要精确 Plan Hash 和本地写确认，且必须先备份。
- `upload` 需要成功 Local ApplyReceipt、精确 Hash 和生产上传确认，使用完整 `qing_daoke.xlsx` 覆盖既有临时表。

2026-07-27 验证样本的活动表为 `0728期`，90 行；与本地 `20260728期` 的 90 行有效值一致，因此正确预检结果为 no-op。
