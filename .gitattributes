# Keep the canonical instructions deterministic across Windows checkouts.
AGENTS.md text eol=lf

# Market canonical SQL is an exact production-source snapshot; preserve its remote hash.
market-consultant-dashboard-sql/resources/raw_sql/*.sql whitespace=-blank-at-eol,-space-before-tab
