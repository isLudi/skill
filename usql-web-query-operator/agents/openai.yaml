interface:
  display_name: "USQL Web Query Operator"
  short_description: "Run governed USQL workflows, inspect BI dashboards, and safely plan or explicitly apply public-filter edits."
  default_prompt: "Use $usql-web-query-operator to run safe SQL, fetch or download Template Query results, upload authorized temp tables, sync governed knowledge, inspect BI dashboards, or dry-run and explicitly apply authorized public-filter edits while preserving login state and safety gates."
