interface:
  display_name: "USQL Web Query Operator"
  short_description: "执行 USQL，并治理模板、Data Center、Taitan 与 Tiangong2 只读探查。"
  default_prompt: "Use $usql-web-query-operator only as the platform execution layer. Route business knowledge through the validated domain adapter registry, require the matching domain SQL skill for business semantics, and preserve read-only defaults, exact hashes, explicit write confirmations, structured receipts, permanent-template create versus separate publish and readback, dashboard draft Apply versus separate Publish, Data Center fresh SUCCESS verification, download limits, login-state isolation, and capability allowlists. For Tiangong2 task exploration, use only the exact scoped credential section, the isolated state, the read-only endpoint allowlist, runtime-only redacted artifacts, and never run, save, submit, publish, create, modify, delete, or write discovered code into knowledge. Read only the reference selected by SKILL.md for the requested command."
