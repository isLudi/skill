interface:
  display_name: "USQL Web Query Operator"
  short_description: "Run USQL and govern or probe hash-bound Taitan changes."
  default_prompt: "Use $usql-web-query-operator to run safe SQL with optional QueryPlan validation, fetch or download Template Query results, inspect BI dashboards, audit P4A/P4B dashboard write capabilities in an explicit sandbox, or follow the independent profile, design, dry-run, verified draft-apply with reverse recovery, and publish-confirmation chain while preserving domain, hash, login-state, download, and dashboard-write safety gates."
