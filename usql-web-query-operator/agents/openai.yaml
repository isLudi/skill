interface:
  display_name: "USQL Web Query Operator"
  short_description: "Run USQL and govern Data Center or Taitan build/change Sagas."
  default_prompt: "Use $usql-web-query-operator to run safe SQL, fetch or download Template Query results, read and locally sync Data Center SQL, plan or explicitly apply hash-bound Data Center replace or create-preview-schedule-save-refresh workflows, inspect BI dashboards, audit P4A/P4B/P4C write capabilities, follow the independent Taitan profile-design-dry-run-draft-apply-publish chain for existing dashboards, or plan and govern the separate no-auto-delete DashboardBuild Saga for a uniquely named new dashboard while preserving domain, identity, upstream artifact, schema, evidence, confirmation, login-state, download, and write-safety gates."
