interface:
  display_name: "USQL Web Query Operator"
  short_description: "Run governed USQL web queries through Playwright."
  default_prompt: "Use the USQL web query operator to run a safe SQL query through the company web query page, preserving login state and respecting row/download limits."
