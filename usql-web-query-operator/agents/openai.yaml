interface:
  display_name: "USQL Web Query Operator"
  short_description: "Run USQL and govern hash-bound Data Center or Taitan changes."
  default_prompt: "Use $usql-web-query-operator to run safe SQL, fetch or download Template Query results, read and locally sync Data Center SQL, plan or explicitly apply hash-bound Data Center replace or create-preview-schedule-save-refresh workflows, inspect BI dashboards, audit P4A/P4B write capabilities, or follow the independent Taitan profile, design, dry-run, verified draft-apply, and publish-confirmation chain while preserving domain, identity, hash, login-state, download, and write-safety gates."
