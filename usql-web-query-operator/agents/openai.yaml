interface:
  display_name: "USQL Web Query Operator"
  short_description: "Run governed USQL web queries, upload temp tables, and sync Data Map or Data Center knowledge."
  default_prompt: "Use the USQL web query operator to run safe SQL through the company web query page, upload authorized temp tables, refresh Data Map field docs, or sync Data Center dataset source SQL while preserving login state and local safety gates."
